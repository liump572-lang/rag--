[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -p no:warnings
markers =
    slow: 需要较长时间运行的测试
    integration: 集成测试（依赖外部服务）
    unit: 纯单元测试
