# 另一台电脑部署说明

本文档说明如何在一台新的 Windows 电脑上，从 GitHub 克隆项目并一键部署运行。

项目仓库地址：

```text
https://github.com/liump572-lang/rag--.git
```

## 一、部署前准备

### 1. 安装 Docker Desktop

在另一台电脑上先安装 Docker Desktop：

```text
https://www.docker.com/products/docker-desktop/
```

安装完成后启动 Docker Desktop，等待左下角或状态栏显示 Docker 正在运行。

验证 Docker 是否可用：

执行目录：任意目录都可以。

```powershell
docker version
docker compose version
```

能正常输出版本信息，说明 Docker 可用。

### 2. 安装 Git

如果电脑没有 Git，先安装：

```text
https://git-scm.com/downloads
```

验证 Git 是否可用：

执行目录：任意目录都可以。

```powershell
git --version
```

## 二、克隆项目

选择一个你想存放项目的目录，例如 `D:\projects`。

执行目录：

```text
D:\projects
```

如果目录不存在，先创建：

```powershell
mkdir D:\projects
cd D:\projects
```

克隆仓库：

```powershell
git clone https://github.com/liump572-lang/rag--.git
```

进入项目根目录：

```powershell
cd rag--
```

后续命令如果没有特别说明，都在这个目录执行：

```text
D:\projects\rag--
```

## 三、填写环境变量

仓库里已经带有 Docker 部署用的环境文件：

```text
docker\.env
```

另一台电脑部署时，主要需要填写 DeepSeek API Key。

执行目录：

```text
D:\projects\rag--
```

打开环境文件：

```powershell
notepad docker\.env
```

找到这一行：

```env
DEEPSEEK_API_KEY=
```

把它改成自己的 DeepSeek API Key，例如：

```env
DEEPSEEK_API_KEY=sk-你的key
```

保存并关闭。

注意：

- `DEEPSEEK_API_KEY` 必须由部署者自己填写。
- 不要把自己的真实 API Key 发给别人。
- 其他默认配置可以先不改，直接部署。

## 四、一键部署

执行目录：

```text
D:\projects\rag--
```

运行一键部署脚本：

```powershell
.\deploy.ps1
```

脚本会自动完成这些事情：

- 检查 Docker 是否可用。
- 使用 `docker\.env` 作为环境配置。
- 构建后端镜像。
- 构建前端镜像。
- 启动 MySQL、Redis、Neo4j、ChromaDB。
- 启动 FastAPI 后端。
- 启动 Celery 文档处理 Worker。
- 启动 Celery 知识图谱 Worker。
- 启动 Nginx 前端服务。
- 等待前端和后端健康检查通过。

首次部署会下载 Docker 镜像并构建项目，时间可能较长，通常需要几分钟。

## 五、部署成功后访问

部署成功后，浏览器打开：

| 地址 | 作用 |
| --- | --- |
| `http://localhost` | 系统前端页面 |
| `http://localhost:8000/docs` | 后端接口文档 |
| `http://localhost:8000/api/v1/health` | 后端健康检查 |
| `http://localhost:7474/browser/` | Neo4j 图数据库页面 |

Neo4j 默认登录信息在 `docker\.env` 中：

```env
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4j_password
```

默认登录：

```text
用户名：neo4j
密码：neo4j_password
```

## 六、验证部署是否正常

### 1. 查看容器状态

执行目录：

```text
D:\projects\rag--
```

命令：

```powershell
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
```

正常情况下应该看到这些容器：

```text
kqa-frontend
kqa-backend
kqa-celery-worker
kqa-celery-kg-worker
kqa-mysql
kqa-redis
kqa-neo4j
kqa-chromadb
```

其中 `kqa-frontend`、`kqa-backend`、`kqa-mysql`、`kqa-neo4j`、`kqa-chromadb` 通常会显示 `healthy`。

### 2. 检查后端健康

执行目录：

```text
D:\projects\rag--
```

命令：

```powershell
Invoke-WebRequest -UseBasicParsing http://localhost:8000/api/v1/health
```

正常返回类似：

```json
{"code":200,"message":"success","data":{"status":"ok","version":"1.0.0"}}
```

### 3. 检查前端

浏览器打开：

```text
http://localhost
```

能看到登录页面，说明前端部署成功。

## 七、常用启动和停止命令

### 1. 启动服务

执行目录：

```text
D:\projects\rag--
```

命令：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml up -d
```

### 2. 停止服务

执行目录：

```text
D:\projects\rag--
```

命令：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml down
```

### 3. 重启服务

执行目录：

```text
D:\projects\rag--
```

命令：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml restart
```

### 4. 重新构建并启动

修改代码后需要重新构建：

执行目录：

```text
D:\projects\rag--
```

命令：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml up -d --build
```

## 八、清空数据重新部署

如果想清空 MySQL、Neo4j、ChromaDB、Redis 等数据后重新部署：

执行目录：

```text
D:\projects\rag--
```

命令：

```powershell
.\deploy.ps1 -ResetData
```

注意：这个命令会删除数据库和上传文件数据，谨慎使用。

## 九、查看日志

所有日志命令执行目录都是：

```text
D:\projects\rag--
```

查看后端日志：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml logs -f backend
```

查看前端日志：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml logs -f frontend
```

查看文档解析 Worker 日志：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml logs -f celery-worker
```

查看知识图谱 Worker 日志：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml logs -f celery-kg-worker
```

查看 Neo4j 日志：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml logs -f neo4j
```

## 十、常见问题

### 1. PowerShell 不允许执行脚本

如果运行：

```powershell
.\deploy.ps1
```

提示脚本执行被禁止，可以在当前 PowerShell 中执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\deploy.ps1
```

这个设置只对当前 PowerShell 窗口生效。

### 2. 端口被占用

本项目默认使用这些端口：

| 端口 | 服务 |
| --- | --- |
| 80 | 前端 Nginx |
| 8000 | 后端 FastAPI |
| 3307 | MySQL |
| 6379 | Redis |
| 7474 | Neo4j 网页 |
| 7687 | Neo4j Bolt |
| 8001 | ChromaDB |

如果端口被占用，需要修改：

```text
docker\docker-compose.yml
```

例如把前端端口从 `80` 改成 `8080`：

```yaml
ports:
  - '8080:80'
```

然后重新部署：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml up -d --build
```

访问地址也改成：

```text
http://localhost:8080
```

### 3. AI 问答或知识图谱抽取失败

优先检查：

```text
docker\.env
```

确认已经填写：

```env
DEEPSEEK_API_KEY=sk-你的key
```

然后重启后端和 Worker：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml restart backend celery-worker celery-kg-worker
```

### 4. 前端页面打不开

检查前端容器：

```powershell
docker ps --filter "name=kqa-frontend"
```

查看日志：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml logs -f frontend
```

### 5. 后端接口打不开

检查后端容器：

```powershell
docker ps --filter "name=kqa-backend"
```

查看日志：

```powershell
docker compose --env-file docker\.env -f docker\docker-compose.yml logs -f backend
```

### 6. Docker 构建失败

可以关闭 BuildKit 后重试：

```powershell
$env:DOCKER_BUILDKIT='0'
$env:COMPOSE_DOCKER_CLI_BUILD='0'
docker compose --env-file docker\.env -f docker\docker-compose.yml up -d --build
```

## 十一、另一台电脑部署最短流程

如果 Docker Desktop 和 Git 都已经安装好，最短流程如下：

执行目录：

```text
D:\projects
```

命令：

```powershell
git clone https://github.com/liump572-lang/rag--.git
cd rag--
notepad docker\.env
```

填写：

```env
DEEPSEEK_API_KEY=自己的 DeepSeek API Key
```

然后执行：

```powershell
.\deploy.ps1
```

部署完成后打开：

```text
http://localhost
```
