# 计算机学科知识点智能问答系统部署与文件说明

本文档用于项目部署、启动、验证、故障排查和代码文件说明。所有命令都写明了执行目录，按步骤执行即可。

## 一、项目根目录

本文档中的“项目根目录”指：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

如果项目放在其他位置，请进入你自己的项目目录。例如：

```powershell
cd D:\vxcode\代码文件\实习文件夹\沛沛沛
```

## 二、部署方式选择

推荐使用 Docker Compose 部署，因为系统包含多个服务：

| 服务 | 作用 |
| --- | --- |
| frontend | Vue 前端页面，由 Nginx 提供访问 |
| backend | FastAPI 后端接口 |
| celery-worker | 文档解析、向量化等异步任务 |
| celery-kg-worker | 知识图谱抽取任务 |
| mysql | 业务关系数据库 |
| redis | Celery 消息队列和缓存 |
| neo4j | 知识图谱数据库 |
| chromadb | 向量数据库 |

## 三、环境要求

### 1. 必装软件

| 软件 | 建议版本 | 说明 |
| --- | --- | --- |
| Docker Desktop | 最新稳定版 | Windows 本地部署推荐 |
| Docker Compose | v2 | Docker Desktop 自带 |
| Git | 最新稳定版 | 拉取项目代码 |

### 2. 推荐机器配置

| 项目 | 建议 |
| --- | --- |
| CPU | 4 核以上 |
| 内存 | 8GB 以上，推荐 16GB |
| 磁盘 | 20GB 以上可用空间 |
| 网络 | 能访问 DeepSeek API |

## 四、首次部署

### 1. 进入项目根目录

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
cd D:\vxcode\代码文件\实习文件夹\沛沛沛
```

### 2. 配置环境变量

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

如果 `docker\.env` 不存在，先复制模板：

```powershell
Copy-Item docker\.env.example docker\.env
```

然后打开并编辑：

```powershell
notepad docker\.env
```

重点检查这些配置：

```env
MYSQL_ROOT_PASSWORD=root123
MYSQL_DATABASE=knowledge_qa_system
MYSQL_USER=qa_app
MYSQL_PASSWORD=qa_app_pass

NEO4J_AUTH=neo4j/neo4j_password
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4j_password

DEEPSEEK_API_KEY=你的 DeepSeek API Key
DEEPSEEK_API_BASE=https://api.deepseek.com/v1
LLM_MODEL=deepseek-v4-flash
EMBEDDING_MODEL=deepseek-embedding
EMBEDDING_DIM=1024

JWT_SECRET_KEY=请改成随机长字符串
```

说明：

- `DEEPSEEK_API_KEY` 不配置时，问答、向量化、知识图谱抽取可能无法正常工作。
- `NEO4J_AUTH` 的密码必须和 `NEO4J_PASSWORD` 保持一致。
- `JWT_SECRET_KEY` 生产环境必须修改，不能用默认值。
- `docker\.env` 不要提交到 Git。

### 3. 一键部署

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
.\deploy.ps1
```

脚本会自动执行以下动作：

- 检查 Docker 是否可用。
- 检查或创建 `docker\.env`。
- 构建后端、前端和 Celery 镜像。
- 启动 MySQL、Redis、Neo4j、ChromaDB、后端、前端和任务 Worker。
- 等待服务健康检查通过。

### 4. 手动部署方式

如果不用脚本，也可以手动执行。

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker compose -f docker\docker-compose.yml up -d --build
```

如果 Docker BuildKit 报错，可以改用经典构建方式：

```powershell
$env:DOCKER_BUILDKIT='0'
$env:COMPOSE_DOCKER_CLI_BUILD='0'
docker compose -f docker\docker-compose.yml up -d --build
```

## 五、启动、停止、重启

### 1. 启动全部服务

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker compose -f docker\docker-compose.yml up -d
```

### 2. 停止全部服务

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker compose -f docker\docker-compose.yml down
```

### 3. 重启全部服务

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker compose -f docker\docker-compose.yml restart
```

### 4. 修改代码后重新构建

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker compose -f docker\docker-compose.yml up -d --build backend celery-worker celery-kg-worker frontend
```

说明：

- 后端代码在容器中挂载了项目目录，很多情况下会自动 reload。
- 前端生产容器使用 Nginx 静态文件，修改 `frontend` 后必须重新构建 `frontend` 镜像。

### 5. 清空数据后重新部署

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
.\deploy.ps1 -ResetData
```

这个命令会删除 Docker 数据卷，MySQL、Neo4j、ChromaDB、Redis 中的数据都会被清空。

## 六、服务访问地址

| 地址 | 说明 |
| --- | --- |
| http://localhost | 前端页面 |
| http://localhost/admin/knowledge-graph | 管理端知识图谱页面 |
| http://localhost:8000/docs | 后端 Swagger API 文档 |
| http://localhost:8000/api/v1/health | 后端健康检查 |
| http://localhost:7474/browser/ | Neo4j 浏览器 |
| http://localhost:8001 | ChromaDB 服务端口 |

Neo4j 默认登录：

```text
用户名：neo4j
密码：docker\.env 中的 NEO4J_PASSWORD
```

## 七、部署后验证

### 1. 查看容器状态

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
```

应该看到这些容器：

```text
kqa-frontend
kqa-backend
kqa-celery-worker
kqa-celery-kg-worker
kqa-mysql
kqa-redis
kqa-neo4j
kqa-chromadb
```

### 2. 检查后端健康

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
Invoke-WebRequest -UseBasicParsing http://localhost:8000/api/v1/health
```

正常会返回：

```json
{"code":200,"message":"success","data":{"status":"ok","version":"1.0.0"}}
```

### 3. 检查前端页面

浏览器打开：

```text
http://localhost
```

如果能看到登录页，说明前端 Nginx 正常。

### 4. 检查 Neo4j

浏览器打开：

```text
http://localhost:7474/browser/
```

登录后执行：

```cypher
MATCH (n) RETURN count(n);
```

如果刚清空数据，节点数应该为 0。上传文档并完成知识抽取后，这里会出现知识点节点。

## 八、常用日志命令

所有命令执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

查看后端日志：

```powershell
docker compose -f docker\docker-compose.yml logs -f backend
```

查看前端日志：

```powershell
docker compose -f docker\docker-compose.yml logs -f frontend
```

查看普通 Celery Worker 日志：

```powershell
docker compose -f docker\docker-compose.yml logs -f celery-worker
```

查看知识图谱 Celery Worker 日志：

```powershell
docker compose -f docker\docker-compose.yml logs -f celery-kg-worker
```

查看 MySQL 日志：

```powershell
docker compose -f docker\docker-compose.yml logs -f mysql
```

查看 Neo4j 日志：

```powershell
docker compose -f docker\docker-compose.yml logs -f neo4j
```

## 九、常用维护命令

### 1. 进入 MySQL

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker exec -it kqa-mysql mysql -uroot -proot123 knowledge_qa_system
```

常用 SQL：

```sql
SELECT COUNT(*) FROM documents;
SELECT COUNT(*) FROM knowledge_points;
SELECT COUNT(*) FROM knowledge_relations;
```

### 2. 进入 Neo4j 命令行

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker exec -it kqa-neo4j cypher-shell -u neo4j -p neo4j_password
```

常用 Cypher：

```cypher
MATCH (n) RETURN count(n);
MATCH ()-[r]->() RETURN count(r);
MATCH (n) DETACH DELETE n;
```

### 3. 重新构建前端

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker compose -f docker\docker-compose.yml up -d --build frontend
```

### 4. 重新构建后端和任务服务

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker compose -f docker\docker-compose.yml up -d --build backend celery-worker celery-kg-worker
```

## 十、本地开发方式

Docker 完整部署适合运行系统。本地开发时，可以只用 Docker 启动数据库和中间件，然后在宿主机运行后端、前端。

### 1. 启动基础设施

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
docker compose -f docker\docker-compose.yml up -d mysql redis neo4j chromadb
```

### 2. 后端本地运行

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

创建虚拟环境：

```powershell
python -m venv .venv
```

激活虚拟环境：

```powershell
.\.venv\Scripts\Activate.ps1
```

安装依赖：

```powershell
pip install -r requirements.txt
```

复制根目录环境变量：

```powershell
Copy-Item docker\.env.example .env
```

编辑 `.env`，把容器内地址改成本机地址：

```env
DATABASE_URL=mysql+pymysql://qa_app:qa_app_pass@localhost:3307/knowledge_qa_system?charset=utf8mb4
REDIS_HOST=localhost
REDIS_PORT=6379
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/1
NEO4J_URI=bolt://localhost:7687
CHROMA_HOST=localhost
CHROMA_PORT=8001
UPLOAD_DIR=uploads
```

启动后端：

```powershell
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 3. 前端本地运行

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛\frontend
```

命令：

```powershell
cd D:\vxcode\代码文件\实习文件夹\沛沛沛\frontend
npm install
npm run dev
```

前端开发地址一般是：

```text
http://localhost:5173
```

### 4. 本地运行测试

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛
```

命令：

```powershell
python -m pytest
```

只测试知识库和知识图谱：

```powershell
python -m pytest tests\test_kb.py tests\test_kg.py
```

前端构建测试：

执行目录：

```text
D:\vxcode\代码文件\实习文件夹\沛沛沛\frontend
```

命令：

```powershell
npm run build
```

## 十一、主要文件和目录作用

### 1. 根目录文件

| 文件/目录 | 作用 |
| --- | --- |
| `README.md` | 项目总体介绍、功能说明和基础使用说明 |
| `DEPLOYMENT.md` | 当前部署文档，包含启动、验证、维护和文件说明 |
| `requirements.txt` | Python 后端依赖列表，后端镜像和本地开发都会使用 |
| `pytest.ini` | Pytest 测试配置 |
| `deploy.ps1` | Windows 一键部署脚本 |
| `docker-rebuild.ps1` | Docker 镜像重建辅助脚本 |
| `.env` | 本地开发环境变量，后端本地启动时读取 |
| `.gitignore` | Git 忽略规则 |
| `.dockerignore` | Docker 构建上下文忽略规则 |
| `uploads/` | 上传文档保存目录 |
| `knowledge_docs/` | 项目知识文档或资料目录 |
| `tests/` | 自动化测试目录 |
| `frontend/` | Vue 前端项目 |
| `app/` | FastAPI 后端项目 |
| `docker/` | Docker 部署相关文件 |
| `tools/` | 辅助脚本目录 |

### 2. `docker/` 目录

| 文件 | 作用 |
| --- | --- |
| `docker/docker-compose.yml` | Docker Compose 编排文件，定义 MySQL、Redis、Neo4j、ChromaDB、后端、前端、Worker |
| `docker/.env.example` | Docker 环境变量模板 |
| `docker/.env` | Docker 实际环境变量，部署时读取 |
| `docker/Dockerfile.backend` | 后端 FastAPI 镜像构建文件 |
| `docker/Dockerfile.celery` | Celery Worker 镜像构建文件 |
| `docker/Dockerfile.frontend` | 前端生产镜像构建文件，构建 Vue 并复制到 Nginx |
| `docker/Dockerfile.frontend.dev` | 前端开发镜像构建文件 |
| `docker/nginx.conf` | Nginx 配置，负责前端静态资源和 API 反向代理 |
| `docker/init.sql` | MySQL 初始化 SQL |
| `docker/fix_garbled.sql` | 数据库乱码修复辅助 SQL |

### 3. `app/` 后端目录

| 文件/目录 | 作用 |
| --- | --- |
| `app/main.py` | FastAPI 应用入口，注册路由、中间件、启动初始化逻辑 |
| `app/config.py` | 后端配置类，读取 `.env` 或容器环境变量 |
| `app/database.py` | SQLAlchemy 数据库连接和 Session 配置 |
| `app/models.py` | 全局数据库 ORM 模型定义 |
| `app/seed.py` | 初始化基础用户或演示数据的脚本 |
| `app/seed_kg.py` | 初始化知识图谱演示数据的脚本 |
| `app/common/` | 后端公共能力目录 |
| `app/middleware/` | 中间件目录 |
| `app/modules/` | 业务模块目录 |
| `app/tasks/` | Celery 异步任务目录 |

### 4. `app/common/` 公共能力

| 文件/目录 | 作用 |
| --- | --- |
| `app/common/response.py` | 统一 API 响应格式 |
| `app/common/exceptions.py` | 统一异常定义 |
| `app/common/utils.py` | 通用工具函数 |
| `app/common/llm_client.py` | DeepSeek/OpenAI 兼容接口调用封装 |
| `app/common/vector_store.py` | ChromaDB 向量库封装 |
| `app/common/graph_store.py` | Neo4j 图数据库封装 |
| `app/common/kg_local_extractor.py` | 本地知识点/关系抽取辅助逻辑 |
| `app/common/kg_settings.py` | 知识图谱抽取阈值等配置 |
| `app/common/schema_migrations.py` | 启动时数据库结构补齐和索引创建 |
| `app/common/parsers/` | 文档解析和切分模块 |

### 5. `app/common/parsers/` 文档解析

| 文件 | 作用 |
| --- | --- |
| `chunker.py` | 将文档文本切分成适合向量化和知识抽取的分段 |
| `cleaner.py` | 清洗解析出的文本 |
| `pdf_parser.py` | 解析 PDF 文档 |
| `docx_parser.py` | 解析 Word DOCX 文档 |
| `pptx_parser.py` | 解析 PowerPoint PPTX 文档 |
| `txt_parser.py` | 解析 TXT 文档 |
| `md_parser.py` | 解析 Markdown 文档 |
| `__init__.py` | 解析器包入口 |

### 6. `app/middleware/` 中间件

| 文件 | 作用 |
| --- | --- |
| `auth.py` | JWT 鉴权中间件和用户身份解析 |
| `cors.py` | 跨域配置 |

### 7. `app/modules/` 业务模块

每个业务模块通常包含：

| 文件 | 作用 |
| --- | --- |
| `router.py` | API 路由入口 |
| `service.py` | 业务逻辑 |
| `schemas.py` | Pydantic 请求和响应模型 |
| `models.py` | 模块局部模型或兼容导入 |
| `__init__.py` | Python 包标记 |

模块说明：

| 目录 | 作用 |
| --- | --- |
| `app/modules/auth/` | 登录、注册、令牌刷新、密码校验 |
| `app/modules/user/` | 用户管理 |
| `app/modules/kb/` | 知识库管理、文档上传、删除、重解析 |
| `app/modules/kg/` | 知识图谱实体、关系、重建、进度、可视化接口 |
| `app/modules/qa/` | 智能问答、检索增强生成、会话消息 |
| `app/modules/notes/` | 学习心得/笔记发布与审核 |
| `app/modules/wrong_q/` | 错题管理 |
| `app/modules/dashboard/` | 管理后台统计数据 |
| `app/modules/notification/` | 通知消息 |
| `app/modules/system/` | 系统配置，例如知识图谱抽取阈值 |

### 8. `app/tasks/` 异步任务

| 文件 | 作用 |
| --- | --- |
| `celery_app.py` | Celery 应用配置，连接 Redis |
| `document_parse.py` | 文档解析任务 |
| `vectorize.py` | 文档分段向量化任务 |
| `kg_extract.py` | 知识点和关系抽取任务 |
| `kg_rebuild.py` | 知识图谱全量重建和同步修复任务 |

### 9. `frontend/` 前端目录

| 文件/目录 | 作用 |
| --- | --- |
| `frontend/package.json` | 前端依赖和脚本配置 |
| `frontend/package-lock.json` | npm 依赖锁定文件 |
| `frontend/index.html` | Vite 入口 HTML |
| `frontend/vite.config.js` | Vite 构建配置 |
| `frontend/src/` | 前端源码 |
| `frontend/dist/` | 前端构建产物 |
| `frontend/node_modules/` | 前端依赖目录，本地安装后生成，不需要提交 |

### 10. `frontend/src/` 前端源码

| 文件/目录 | 作用 |
| --- | --- |
| `frontend/src/main.js` | Vue 应用入口 |
| `frontend/src/App.vue` | 根组件 |
| `frontend/src/router/index.js` | 前端路由配置 |
| `frontend/src/stores/auth.js` | Pinia 登录状态和用户信息 |
| `frontend/src/api/` | 前端 API 请求封装 |
| `frontend/src/components/` | 通用组件 |
| `frontend/src/composables/` | Vue 组合式函数 |
| `frontend/src/styles/theme.css` | 全局主题样式 |
| `frontend/src/utils/markdown.js` | Markdown 渲染工具 |
| `frontend/src/views/` | 页面组件 |

### 11. `frontend/src/api/` API 文件

| 文件 | 作用 |
| --- | --- |
| `request.js` | Axios 实例、Token 注入、响应拦截 |
| `auth.js` | 登录注册相关接口 |
| `user.js` | 用户管理接口 |
| `kb.js` | 知识库和文档接口 |
| `kg.js` | 知识图谱接口 |
| `qa.js` | 智能问答接口 |
| `notes.js` | 学习心得接口 |
| `notification.js` | 通知接口 |
| `subjects.js` | 科目接口 |
| `systemConfig.js` | 系统配置接口 |
| `wrongQuestions.js` | 错题接口 |

### 12. `frontend/src/views/` 页面

| 目录/文件 | 作用 |
| --- | --- |
| `Login.vue` | 登录页 |
| `admin/` | 管理端页面 |
| `user/` | 用户端页面 |

管理端常见页面：

| 文件 | 作用 |
| --- | --- |
| `admin/KnowledgeBase.vue` | 知识库管理，上传、删除、重解析文档 |
| `admin/KnowledgeGraph.vue` | 知识图谱可视化、实体关系管理、抽取进度 |
| `admin/SubjectManage.vue` | 科目管理 |
| `admin/UserManage.vue` | 用户管理 |
| `admin/NoteReview.vue` | 心得审核 |
| `admin/SystemConfig.vue` | 系统配置 |

用户端常见页面：

| 文件 | 作用 |
| --- | --- |
| `user/QaChat.vue` | 智能问答聊天页面 |
| `user/MyNotes.vue` | 我的心得 |
| `user/StudyNotes.vue` | 心得列表 |
| `user/NoteDetail.vue` | 心得详情 |
| `user/WrongQuestions.vue` | 错题页面 |

### 13. `tests/` 测试目录

| 文件/目录 | 作用 |
| --- | --- |
| `tests/conftest.py` | 测试夹具和公共测试配置 |
| `tests/test_auth.py` | 登录注册测试 |
| `tests/test_kb.py` | 知识库测试 |
| `tests/test_kg.py` | 知识图谱测试 |
| `tests/test_qa.py` | 智能问答测试 |
| `tests/test_models.py` | ORM 模型测试 |
| `tests/test_parsers.py` | 文档解析测试 |
| `tests/test_celery_tasks.py` | 异步任务测试 |
| `tests/test_system.py` | 系统配置测试 |
| `tests/test_wrong_q.py` | 错题模块测试 |
| `tests/test_notes.py` | 心得模块测试 |
| `tests/test_notification.py` | 通知模块测试 |
| `tests/test_dashboard.py` | 工作台统计测试 |
| `tests/fixtures/` | 测试数据 |
| `tests/测试方案.md` | 测试方案说明 |

## 十二、常见问题

### 1. 端口被占用

如果 `80`、`8000`、`7474`、`7687`、`3307`、`6379` 或 `8001` 被占用，需要修改 `docker/docker-compose.yml` 中的端口映射。

例如前端端口从 `80` 改成 `8080`：

```yaml
ports:
  - '8080:80'
```

然后重新启动：

```powershell
docker compose -f docker\docker-compose.yml up -d
```

### 2. Neo4j 有两个端口

Neo4j 暴露两个端口是正常的：

| 端口 | 作用 |
| --- | --- |
| 7474 | 浏览器管理界面，访问 `http://localhost:7474/browser/` |
| 7687 | Bolt 协议端口，后端程序连接 Neo4j 使用 |

### 3. 修改前端后页面没变化

生产部署中前端是 Nginx 静态文件，必须重新构建前端镜像：

```powershell
docker compose -f docker\docker-compose.yml up -d --build frontend
```

然后浏览器强制刷新：

```text
Ctrl + F5
```

### 4. 知识图谱抽取很慢

常见原因：

- 文档过大或切块太多。
- LLM API 响应慢。
- Celery 并发不足。
- Neo4j 或 MySQL 写入压力较大。

可以查看：

```powershell
docker compose -f docker\docker-compose.yml logs -f celery-kg-worker
```

### 5. 删除文档后 Neo4j 还有实体

当前代码已经增加删除后的图谱对齐清理。排查命令：

```powershell
docker exec kqa-neo4j cypher-shell -u neo4j -p neo4j_password "MATCH (n:KnowledgePoint) RETURN count(n) AS nodes"
docker exec kqa-neo4j cypher-shell -u neo4j -p neo4j_password "MATCH (:KnowledgePoint)-[r:RELATED]->(:KnowledgePoint) RETURN count(r) AS relations"
```

如果需要强制清空 Neo4j：

```powershell
docker exec kqa-neo4j cypher-shell -u neo4j -p neo4j_password "MATCH (n) DETACH DELETE n"
```

## 十三、推荐部署检查清单

部署完成后逐项检查：

- `docker ps` 中所有容器都处于 `Up`，关键容器为 `healthy`。
- `http://localhost` 能打开登录页。
- `http://localhost:8000/api/v1/health` 返回 `code=200`。
- `http://localhost:8000/docs` 能打开接口文档。
- `http://localhost:7474/browser/` 可以登录 Neo4j。
- 上传一个测试文档后，文档状态能变为解析成功。
- 知识图谱页面能显示当前文档的抽取进度。
- 问答页面能正常返回回答。
